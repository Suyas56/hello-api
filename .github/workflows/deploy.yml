name: Deploy Hello API

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Copy files to VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.VM_HOST }}
          username: ${{ vars.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: "./!(.git|.github|node_modules|*.log)"
          target: "~/hello-api"
          strip_components: 1
          rm: true

      - name: Deploy on VM
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ vars.VM_HOST }}
          username: ${{ vars.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd ~/hello-api

            echo "📦 Installing dependencies..."
            npm install --omit=dev

            echo "⚙️ Installing pm2 locally if not present..."
            if [ ! -d "node_modules/pm2" ]; then
              npm install pm2 --save
            fi

            echo "🚀 Starting or restarting app with pm2..."
            npx pm2 restart hello-api || npx pm2 start server.js --name hello-api
            npx pm2 save

            echo "✅ Deployment finished!"
